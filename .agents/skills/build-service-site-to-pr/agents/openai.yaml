interface:
  display_name: "Build Service Site to PR"
  short_description: "Build a safe service site around a complete user task"
  default_prompt: "Use $build-service-site-to-pr to approve the task-and-state blueprint or apply the bounded correction loop, preserve safety and asset roles, verify every affected state and renderer, then deliver a cache-checked preview and pull request."
